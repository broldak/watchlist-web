{{watchlist-nav
  movies=model.movies
  fetchAction=(route-action "fetchMovies")
  selectAction=(route-action "selectMovie")
}}

{{#if model.selectedMovie}}
  {{movie-card movie=model.selectedMovie}}
{{/if}}

{{outlet}}
